{% extends 'layouts/base.njk' %}

{% if practice %}
  {% set serviceTitle = 'Register with ' + practice.name %}
{% endif %}

{% block body_main_content %}
  <div class="page-section">
    <h1 class="heading-medium">Thank you for applying. Your application has been sent to {{ practice.name | default('a GP practice') }}</h1>

    <div class="reading-width">
      <p>Contact {{ practice.name | default('your GP practice') }} to tell them your NHS number if you haven’t already provided it.</p>
      <p>Also tell them if you don’t have an NHS number or you can’t find it.</p>
    </div>


    <h2 class="heading-medium">You now need to complete your registration in person</h2>  
    <div class="reading-width">
      <p>Go to <strong>{{ practice.name | default('your GP practice') }}</strong> to:</p>
      <ul>
        <li>sign a copy of your application</li>
        <li>tell them your NHS number if you haven’t already provided it</li>
        <li>tell them if you don’t have an NHS number or you can’t find it.</li>      
      </ul>
    </div>

    <h2 class="heading-medium">How to find your NHS number</h2>
    <p>You can usually find your NHS number:</p>
    <ul>
      <li>on a printed prescription</li>
      <li>on a GP or hospital letter</li>
      <li>by asking your current GP</li>      
    </ul>
    <div class="reading-width">
     {% if practice %}
        <p>
          <strong>{{ practice.name }}</strong>
          {% if practice.contact.address.length %}
            {% set linebreak = joiner('<br>') %}
            {% for address in practice.contact.address %}
              <br>
              {{ linebreak() | safe }}
              {{ address | nl2br | safe }}
            {% endfor %}
          {% endif %}
        </p>

        {% if practice.contact.phone %}
          {% set linebreak = joiner('<br>') %}
          <p>
            {% for phone in practice.contact.phone %}
              {{ linebreak() | safe }}
              Phone: {{ phone }}
            {% endfor %}
          </p>
        {% endif %}
      {% endif %}
    </div>  
  

  <h2 class="heading-medium">After you complete your registration</h2>

    <div class="reading-width">
      <p>Wait X days before making appointments at {{ practice.name | default('your GP practice') }} - this is how long it usually takes to be registered. {{ practice.name | default('your GP practice') }} will contact you if they need anything else from you.
      Also tell them if you don’t have an NHS number or you can’t find it.
      </p>
      
      <h5>If you need an urgent appointment</h5>
      <p>Call {{ practice.name | default('your GP practice') }} to ask about urgent appointments before you’re registered. If they’re closed, call 111 or go to a walk-in centre.</p>
    
      <h5>If you’re on repeat medication</h5>
      <p>Take your repeat prescription or medication to [practice name] as soon as you can. If you’re worried about running out, ask your current GP for a prescription for one month’s supply.</p>

      <h5>If you’re having hospital treatment</h5>
      <p>Tell the hospital that you’ve changed GP. Take a hospital letter or other information about your treatment to {{ practice.name | default('your GP practice') }} as soon as you can.</p>
   </div>

  </div>



{% endblock %}

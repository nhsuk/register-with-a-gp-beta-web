{% extends 'layouts/base.njk' %}

{% set serviceTitle = '' %}

{% block body_main_content %}

  {% block header_section %}
    <div class="local-header">
      <h1 class="local-header__title">{% block header_section_title %} Register with {{ CURRENT_PRACTICE.name | default('the GP practice') }} {% endblock %}</h1>
    </div>
  {% endblock %}

  <div class="page-section">
    {% block body_main_content_main %}
    <p>You can apply online if you’re registering for yourself.</p>
    <div class="reading-width">
      {% if CURRENT_PRACTICE.contact.link %}
          <section class="callout callout--info">
            <div class="reading-with">
              <p>
                To register for someone else, you need to go in person to <a href="{{ CURRENT_PRACTICE.contact.link }}">{{ CURRENT_PRACTICE.name }}</a>.
              </p>
              </div>
          </section>
      {% endif %}

      <h3>How long it takes</h3>
      <p>
        {% block how_long_takes_paragraph %}
          Applying online takes about 10 minutes. You’ll then need to go to {{ CURRENT_PRACTICE.name }} with proof of your identity and address. It usually takes {{ CURRENT_PRACTICE.regdays | default(7) }} days after that before you’re registered.
        {% endblock %}
      </p>

      <section class="callout callout--info">
        <div class="reading-with">
          <p>
            Some GP practices only register people who live in the local area. Check with <a href="{{ CURRENT_PRACTICE.contact.link }}">{{ CURRENT_PRACTICE.name }}</a> if you’re unsure.
          </p>
          </div>
      </section>

      {% endblock %}
      <details>
        <summary>
          <span class="details__summary">How your information is used</span>
        </summary>
        <div class="details__content">
          <div class="callout callout--info">
            <p>Only the GP surgery sees the information in your application form. It’s sent securely so they can add it to your medical record. It’s not used for any other purpose.</p>
          </div>
        </div>
      </details>

      <a href="{{ firstStep }}" class="button start-step-btn" id="start-button">Start</a>
    </div>
  </div>
  
{% endblock %}

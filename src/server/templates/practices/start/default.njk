{% extends 'layouts/base.njk' %}

{% set serviceTitle = '' %}

{% block body_main_content %}

  {% block header_section %}
    <div class="local-header">
      <h1 class="local-header__title">{% block header_section_title %} Register with {{ CURRENT_PRACTICE.name | default('the GP practice') }} {% endblock %}</h1>
    </div>
  {% endblock %}

  <div class="page-section">
    <div class="reading-width register-gp-start">
      {% block body_main_content_steps %}
        <p>To register: </p>
        <ol>
          <li>Fill out this form (takes about 10 minutes) </li>
          <li>Go to the surgery with proof of ID and address</li>
        </ol>
      {% endblock %}

      <p>You should be registered in [x] days.</p>
      <details>
        <summary>
          <span class="details__summary">How your information is used</span>
        </summary>
        <div class="details__content">
          <div class="callout callout--info">
            <p>Only the GP surgery sees the information in your application form. It’s sent securely so they can add it to your medical record. It’s not used for any other purpose.</p>
          </div>
        </div>
      </details>

      <a href="{{ firstStep }}" class="button start-step-btn" id="start-button">Start now</a>

      {% block who_uses_service %}
        <h3>Who can use this service</h3>
          <p> Use this service to register yourself. To register someone else, go in person to the surgery.</p>
          <p>Some GP practices only register people living in the local area. Check with {{ CURRENT_PRACTICE.name | default('the GP practice') }} if you’re unsure.</p>
      {% endblock %}
      
    </div>
  </div>
{% endblock %}
      
    
  


{% extends 'layouts/base.njk' %}

{% set serviceTitle = '' %}

{% block body_main_content %}
  <div class="local-header">

    {% block body_main_content_title %}
      <h1 class="local-header__title">Register with {{ CURRENT_PRACTICE.name | default('the GP practice') }}</h1>
    {% endblock %}

  </div>

  <div class="page-section">
    {% block body_main_content_main %}
    <p>You can apply online if you’re registering for yourself.</p>
    <div class="reading-width">
      {% if CURRENT_PRACTICE.link %}
          <section class="callout callout--info">
            <div class="reading-with">
              <p>
                To register for someone else, you need to go in person to <a href="{{ CURRENT_PRACTICE.link }}">{{ CURRENT_PRACTICE.name }}</a>.
              </p>
              </div>
          </section>
      {% endif %}

      <h3> How long it takes</h3>
      <p>
        Applying online takes about 10 minutes. You’ll then need to go to [practice] with proof of your identity and address. It usually takes {{ CURRENT_PRACTICE.regdays }} after that before you’re registered.
      </p>

      <a href="{{ CURRENT_PRACTICE.link }}">{{ CURRENT_PRACTICE.name }}</a>

      {% endblock %}
      <details>
        <summary>
          <span class="details__summary">How your personal information is handled</span>
        </summary>
        <div class="details__content" id="details-content-0" aria-hidden="true">
          <div class="callout callout--info">
            <p>Only {{ CURRENT_PRACTICE.name }} sees the personal information you put in this application. It’s sent securely to them so they can add it to your medical record. It’s not stored for any other purpose.</p>
          </div>
        </div>
      </details>

      <a href="{{ firstStep }}" class="button">Start</a>
    </div>
  </div>
{% endblock %}
